name: Build Cornix ZMK BASIC 260817 FIXED v1.2

on:
  workflow_dispatch:
  push:
    paths:
      - "config/**"
      - "dts/**"
      - "src/**"
      - "tools/**"
      - "validation/**"
      - "zephyr/**"
      - "CMakeLists.txt"
      - "Kconfig"
      - "build.yaml"
      - ".github/workflows/build.yml"
    tags-ignore:
      - "*"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@6e2ef41e022d555b10f116e395832913f71717b3
